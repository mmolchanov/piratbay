import React,{useEffect,useMemo,useState}from"react";
import{Area,Bar,CartesianGrid,ComposedChart,Legend,ResponsiveContainer,Tooltip,XAxis,YAxis}from"recharts";
const CSV="https://raw.githubusercontent.com/mmolchanov/piratbay/main/request_volume_fact.csv";
const dims=["business","desk","sub_desk","analyst","manager","status","request_type","salesperson","region","desk_location","cash_vs_deriv"];
const filterOrder=[["business","Business"],["startDate","Start Date"],["endDate","End Date"],["desk","Desk"],["sub_desk","Sub-Desk"],["analyst","Analyst"],["manager","Manager"],["status","Status"],["request_type","Request Type"],["salesperson","Salesperson"],["region","Region"],["desk_location","Desk Location"],["cash_vs_deriv","Cash VS Deriv"]];
const metrics=[["Received","request_date","received_flag","#2F66E8"],["Completed","completed_date","completed_flag","#07184F"],["Cancelled","cancelled_date","cancelled_flag","#62B5F6"],["In Progress","in_progress_date","in_progress_flag","#368C3F"],["On Hold","on_hold_date","on_hold_flag","#D9D9D9"]];
const rowsOrder=["Received","Completed","Cancelled","In Progress","On Hold"],legendOrder=["Received","Cancelled","Completed","In Progress","On Hold"];
function csv(s){const out=[];let r=[],v="",q=false;for(let i=0;i<s.length;i++){const c=s[i],k=c.charCodeAt(0);if(c==='"'){if(q&&s[i+1]==='"'){v+='"';i++}else q=!q}else if(c===","&&!q){r.push(v);v=""}else if((k===10||k===13)&&!q){if(k===13&&s[i+1]&&s[i+1].charCodeAt(0)===10)i++;r.push(v);if(r.some(x=>x!==""))out.push(r);r=[];v=""}else v+=c}r.push(v);if(r.some(x=>x!==""))out.push(r);const h=(out.shift()||[]).map(x=>x.trim());return out.map(a=>Object.fromEntries(h.map((x,i)=>[x,a[i]??""])))}
function dt(x){if(!x)return null;const a=String(x).slice(0,10).split("-").map(Number);return a.length===3&&a[0]&&a[1]&&a[2]?new Date(a[0],a[1]-1,a[2]):null}
function iso(d){return`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`}
function ms(d){return new Date(d.getFullYear(),d.getMonth(),1)}
function add(d,n){return new Date(d.getFullYear(),d.getMonth()+n,1)}
function mon(d){return d.toLocaleDateString("en-US",{month:"short",year:"numeric"})}
function ok(x){return x===1||x===true||String(x).trim().toLowerCase()==="1"||String(x).trim().toLowerCase()==="true"}
function Label(p){return p.value?<text x={p.x+p.width/2} y={p.y-4} textAnchor="middle" className="fill-slate-600 text-[10px]">{p.value}</text>:null}
function Select({label,value,options,onChange}){return <label className="block"><span className="mb-1 block text-xs font-semibold uppercase text-slate-500">{label}</span><select className="w-full rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm shadow-sm" value={value} onChange={e=>onChange(e.target.value)}><option value="(All)">(All)</option>{options.map(o=><option key={o} value={o}>{o}</option>)}</select></label>}
export default function VolumeTrendingSummaryReport(){
const now=new Date(),def={startDate:iso(add(ms(now),-11)),endDate:iso(now)};
const[raw,setRaw]=useState([]),[state,setState]=useState({loading:true,error:""});
useEffect(()=>{let dead=false;(async()=>{try{const r=await fetch(CSV);if(!r.ok)throw new Error(`CSV load failed with status ${r.status}`);const t=await r.text();if(!dead){setRaw(csv(t));setState({loading:false,error:""})}}catch(e){if(!dead)setState({loading:false,error:e.message||"CSV load failed"})}})();return()=>{dead=true}},[]);
const opts=useMemo(()=>Object.fromEntries(dims.map(f=>[f,[...new Set(raw.map(r=>r[f]).filter(Boolean))].sort((a,b)=>String(a).localeCompare(String(b)))])),[raw]);
const[f,setF]=useState({business:"(All)",startDate:def.startDate,endDate:def.endDate,desk:"(All)",sub_desk:"(All)",analyst:"(All)",manager:"(All)",status:"(All)",request_type:"(All)",salesperson:"(All)",region:"(All)",desk_location:"(All)",cash_vs_deriv:"(All)"});
useEffect(()=>{if(!raw.length)return;setF(x=>({...x,business:x.business!=="(All)"?x.business:opts.business?.includes("Corporates")?"Corporates":opts.business?.[0]??"(All)",region:x.region!=="(All)"?x.region:opts.region?.includes("EMEA")?"EMEA":"(All)"}))},[raw.length,opts.business,opts.region]);
const base=useMemo(()=>raw.filter(r=>dims.every(d=>f[d]==="(All)"||r[d]===f[d])),[raw,f]);
const months=useMemo(()=>{const s=dt(f.startDate);return s?Array.from({length:12},(_,i)=>add(ms(s),i)):[]},[f.startDate]);
const data=useMemo(()=>{const s=dt(f.startDate),e=dt(f.endDate),keys=months.map(d=>iso(ms(d))),by=Object.fromEntries(keys.map((k,i)=>[k,{monthKey:k,month:mon(months[i]),Received:0,Completed:0,Cancelled:0,"In Progress":0,"On Hold":0}]));if(!s||!e)return Object.values(by);for(const[m,df,ff]of metrics){const sets={};for(const r of base){const d=dt(r[df]);if(!ok(r[ff])||!d||d<s||d>e)continue;const k=iso(ms(d));if(!by[k])continue;(sets[k]??=new Set()).add(r.request_id)}for(const k in sets)by[k][m]=sets[k].size}return keys.map(k=>by[k])},[base,f.startDate,f.endDate,months]);
const totals=useMemo(()=>Object.fromEntries(rowsOrder.map(m=>[m,data.reduce((s,r)=>s+Number(r[m]||0),0)])),[data]);
const high=m=>data.reduce((b,r)=>Number(r[m]||0)>b.value?{month:r.month,value:Number(r[m]||0)}:b,{month:"-",value:0});
const cancelRate=totals.Received?((totals.Cancelled||0)/totals.Received*100).toFixed(1):"0.0";
const upd=(k,v)=>setF(x=>({...x,[k]:v}));
if(state.loading)return <div className="flex min-h-screen items-center justify-center bg-slate-50 p-6">Loading Volume Trending report...</div>;
if(state.error)return <div className="min-h-screen bg-slate-50 p-6"><div className="rounded-2xl border border-red-200 bg-red-50 p-5 text-red-800">{state.error}</div></div>;
return <section id="dashboard-volume-trending" className="min-h-screen bg-slate-100 p-4 text-slate-900"><div className="mx-auto grid max-w-[1600px] grid-cols-1 gap-4 lg:grid-cols-[300px_1fr]"><aside id="filter-panel" className="rounded-2xl bg-white p-4 shadow-sm"><h2 className="mb-4 text-lg font-semibold">Filters</h2><div className="space-y-3">{filterOrder.map(([k,l])=>k==="startDate"||k==="endDate"?<label key={k} className="block"><span className="mb-1 block text-xs font-semibold uppercase text-slate-500">{l}</span><input className="w-full rounded-xl border border-slate-200 px-3 py-2 text-sm shadow-sm" type="date" value={f[k]} onChange={e=>upd(k,e.target.value)}/></label>:<Select key={k} label={l} value={f[k]} options={opts[k]||[]} onChange={v=>upd(k,v)}/>)}</div></aside><main id="dashboard-main" className="space-y-4"><nav id="dashboard-tabs" className="rounded-2xl bg-white p-2 shadow-sm"><button className="rounded-xl bg-blue-600 px-4 py-2 text-sm font-semibold text-white">Volumes Trending</button></nav><header id="dashboard-header" className="rounded-2xl bg-white p-5 shadow-sm"><h1 className="text-2xl font-bold">Volume Trending</h1><p className="mt-1 text-sm text-slate-600">Business: {f.business} | Region: {f.region} | Period: {months[0]?mon(months[0]):f.startDate} - {dt(f.endDate)?mon(dt(f.endDate)):f.endDate}</p></header><section id="volume-trending-chart" className="rounded-2xl bg-white p-5 shadow-sm"><div className="mb-4 flex flex-wrap items-end justify-between gap-3"><div><h2 className="text-lg font-semibold">Volume Trending</h2><p className="text-sm text-slate-500">Monthly request count by lifecycle status event.</p></div><div className="grid grid-cols-2 gap-2 sm:grid-cols-5">{rowsOrder.map(m=><div key={m} className="rounded-xl border border-slate-200 px-3 py-2 text-right"><div className="text-xs text-slate-500">{m}</div><div className="text-lg font-semibold">{totals[m]||0}</div></div>)}</div></div>{base.length===0?<div className="rounded-xl border border-slate-200 bg-slate-50 p-8 text-center text-sm">No requests match the selected filters.</div>:<div className="h-[420px]"><ResponsiveContainer width="100%" height="100%"><ComposedChart data={data} margin={{top:24,right:24,bottom:12,left:8}}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="month" tick={{fontSize:12}}/><YAxis allowDecimals={false} tick={{fontSize:12}}/><Tooltip formatter={v=>[Number(v).toFixed(0),"Requests"]}/><Legend payload={legendOrder.map(k=>({value:k,type:k==="Received"?"line":"square",color:metrics.find(m=>m[0]===k)?.[3]}))}/><Area type="monotone" dataKey="Received" name="Received" fill="#2F66E8" stroke="#2F66E8" fillOpacity={0.16} strokeWidth={3}/><Bar dataKey="Cancelled" name="Cancelled" fill="#62B5F6" label={<Label/>}/><Bar dataKey="Completed" name="Completed" fill="#07184F" label={<Label/>}/><Bar dataKey="In Progress" name="In Progress" fill="#368C3F" label={<Label/>}/><Bar dataKey="On Hold" name="On Hold" fill="#D9D9D9" label={<Label/>}/></ComposedChart></ResponsiveContainer></div>}</section><section id="monthly-data-table" className="rounded-2xl bg-white p-5 shadow-sm"><h2 className="mb-4 text-lg font-semibold">Monthly Data</h2><div className="overflow-x-auto"><table className="min-w-full border-separate border-spacing-0 text-sm"><thead><tr><th className="sticky left-0 bg-slate-100 px-3 py-2 text-left">Measure</th>{data.map(r=><th key={r.monthKey} className="bg-slate-100 px-3 py-2 text-right">{r.month}</th>)}</tr></thead><tbody>{rowsOrder.map(m=><tr key={m}><td className="sticky left-0 bg-white px-3 py-2 font-medium">{m}</td>{data.map(r=><td key={`${m}-${r.monthKey}`} className="px-3 py-2 text-right tabular-nums">{Number(r[m]||0)}</td>)}</tr>)}</tbody></table></div></section><section className="rounded-2xl bg-white p-5 shadow-sm"><h2 className="mb-3 text-lg font-semibold">Key Observations</h2><ol className="list-decimal space-y-2 pl-5 text-sm text-slate-700"><li>Highest received volume: {high("Received").month} with {high("Received").value} requests.</li><li>Highest in-progress volume: {high("In Progress").month} with {high("In Progress").value} requests.</li><li>Highest on-hold volume: {high("On Hold").month} with {high("On Hold").value} requests.</li><li>Cancellation rate for the period: {cancelRate}%.</li></ol></section></main></div></section>
}